import * as React from "react";

const DescSection = ({ text }) => {
  return (
    <p className="max-w-[520px] font-light text-lg text-custom-gray tracking-negative-2">
      {text}
    </p>
  );
};

export default DescSection;
